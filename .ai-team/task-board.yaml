project: SEIP
updated_at: 2026-07-19T01:00:00+07:00
# The user is the final approver. Multi-agent fields (per-agent branches, allowed
# paths, file locks, cross-agent reviewers) are retired — history in git.
# HARD CONSTRAINT: no production feature code until the Sprint 0 exit gate passes
# (docs/project/SPRINT-0.md, reinterpreted per ADR-0004).
sprint: 1
statuses: [backlog, ready, in_progress, review, done, blocked, cancelled]

tasks:
  - id: SEIP-OPS-001
    title: Bootstrap repository (git, layout, CI skeleton, GitHub)
    status: done                # 2026-07-17 — close-out at .ai-team/handoffs/SEIP-OPS-001.md
    branch: ai/claude/SEIP-OPS-001-repo-bootstrap   # merged to develop
    work_order: .ai-team/work-orders/SEIP-OPS-001.md
    acceptance_criteria:
      - Repo pushed to github.com/sooksun/aischool with main + develop  # DONE
      - CLAUDE.md at root reflects ADR-0004 solo model                  # DONE
      - Ownership validator passes (functional-area map, no overlaps)   # DONE
      - CI runs green on develop (verified via public API)              # DONE 2026-07-17

  - id: SEIP-ARCH-001
    title: Contracts v0.1 draft (openapi, permissions, error-codes, events)
    status: done                # 2026-07-17
    work_order: .ai-team/work-orders/SEIP-ARCH-001.md
    dependencies: [SEIP-OPS-001]
    acceptance_criteria:
      - openapi.yaml events.yaml permissions.yaml error-codes.yaml drafted at v0.1   # DONE
      - Roles + school-scoped tenancy in permissions.yaml                            # DONE
      - Indicator taxonomy per ADR-0003 / architecture/evaluation-framework.md       # DONE
      - OpenAPI lints clean; type generation runs                    # DONE — redocly valid 0 warnings; openapi-typescript OK
      - AI-mapping endpoints explicitly deferred to Sprint 2         # DONE — x-deferred section
      - Resolves CCR-001 (filed by codex pre-ADR-0004)               # DONE — RESOLVED 2026-07-17
      - Module boundary map                                          # DONE — docs/architecture/module-boundaries.md

  - id: SEIP-DB-000
    title: Data model proposal (ERD + entity dictionary, no schema)
    status: done                # 2026-07-17 — review PASS
    work_order: .ai-team/work-orders/SEIP-DB-000.md
    notes: >
      Produced by Codex before ADR-0004; inherited by Claude. Review verdict PASS
      at docs/reviews/SEIP-DB-000-claude-review.md — all 8 framework implications
      covered; contracts v0.1 consistent; 4 non-blocking observations routed to DB-001.
    acceptance_criteria:
      - Claude review pass against evaluation-framework.md implications   # DONE
      - Consistency with contracts v0.1 confirmed in ARCH-001             # DONE

  - id: SEIP-UI-000
    title: Evidence submission UX flow (design, no implementation)
    status: done                # 2026-07-17 — close-out at .ai-team/handoffs/SEIP-UI-000.md
    work_order: .ai-team/work-orders/SEIP-UI-000.md
    dependencies: [SEIP-ARCH-001]
    notes: >
      Found 3 contract gaps during field trace (CCR-002) — applied to draft contract:
      CurrentUser.personnel object, evidence draft→active lifecycle, duration nullable.
    acceptance_criteria:
      - Teacher submits evidence with minimal fields (mobile-first flow)            # DONE — 3 fields
      - All screen states inventoried (empty/loading/error/upload-resume/denied)    # DONE — 8×7 matrix
      - Every UI field traced to a contract field                                   # DONE — 37/37 post CCR-002
      - Accessibility intent documented                                             # DONE — 7 areas

  - id: SEIP-QA-001
    title: Runnable QA and security gate baseline
    status: done                # 2026-07-17 — close-out at .ai-team/handoffs/SEIP-QA-001.md
    work_order: .ai-team/work-orders/SEIP-QA-001.md
    dependencies: [SEIP-OPS-001]
    notes: >
      CI now: 4 LIVE gates (ownership, contracts+oasdiff, gitleaks, dep-audit)
      + 8 self-arming gates that activate when Sprint 1 code lands.
      All four LIVE gates verified locally exit 0 before push.
    acceptance_criteria:
      - Every gate in docs/qa/QUALITY-GATES.md has a runnable command + CI job mapping   # DONE
      - Security baseline + finding severity model created                              # DONE — 28 SEC-* reqs, S1-S5 model

  - id: SEIP-QA-002
    title: Adversarial cross-agent design review
    status: cancelled           # ADR-0004 — absorbed into self-review + user approval
    work_order: .ai-team/work-orders/SEIP-QA-002.md

  - id: SEIP-ARCH-002
    title: Ratify and lock contracts v1.0 (Sprint 0 exit gate)
    status: done                # 2026-07-17 — merged develop b5c6d6a; handoff SEIP-ARCH-002.md
    branch: feat/SEIP-ARCH-002-contract-lock
    dependencies: [SEIP-ARCH-001, SEIP-DB-000, SEIP-UI-000, SEIP-QA-001]
    notes: >
      Contracts locked at 1.0.0. Security pass §2/§3 done (1 S4 finding raised+closed:
      authz coverage gate). Exit gate: 7 pass, 1 accepted deviation (direct commits to
      develop — ADR-0004 sanctions it), 2 superseded, 1 PENDING = item 7 (PR pipeline
      proof). Sprint 1 opens only when this PR is green AND the user approves the merge.
      Close-out: .ai-team/handoffs/SEIP-ARCH-002.md
    acceptance_criteria:
      - Contracts v1.0 locked and versioned                    # DONE — all 4 at 1.0.0
      - User approved                                          # DONE — merge to develop 2026-07-17
      - Generated types refreshed                              # DONE — regenerated by gate:contracts each run; not committed (no consumer until Sprint 1 wires codegen into the build)
      - Definition of Ready to Build satisfied                 # DONE — Sprint 0 exit cleared

# --- Sprint 1: first production features. DO NOT START until ARCH-002 done. ---
  - id: SEIP-DB-001
    title: Core database implementation (schema + migrations + tests)
    status: done                # 2026-07-17 — merged develop be36eff; handoff SEIP-DB-001.md
    branch: feat/SEIP-DB-001-core-database
    dependencies: [SEIP-ARCH-002]
    notes: >
      28-model Prisma schema + 2 migrations (init + constraints). Taxonomy seed
      (v9/v10 structure) included. 18 backend tests pass after migrate reset.
      migration-validation CI armed. ChallengeScore merged into indicator_score.
      Close-out: .ai-team/handoffs/SEIP-DB-001.md
    acceptance_criteria:
      - Configurable evaluation rounds per year                          # DONE — round_number 1..n; test inserts 3
      - Evidence maps many-to-many to indicators (both role taxonomies)  # DONE — mapping + seed T/A codes
      - Append-only audit history per DB-000 design                      # DONE — trigger-enforced, tested
      - Migrations and constraint tests pass                             # DONE — 18/18 after clean reset

  - id: SEIP-DB-002
    title: Seed v9/v10 taxonomy (frameworks, indicators, level descriptions, weights)
    status: done                # structure/codes/weights verified (seed-taxonomy tests); residual = full level text from PDFs only
    dependencies: [SEIP-DB-001]
    notes: >
      Structure seed shipped with DB-001 (19 indicators per role, weights, ranks,
      evidence categories). Full IndicatorLevelDescription wording from official
      PDFs is residual data load — not blocking schema or UI-001.
    acceptance_criteria:
      - Both frameworks seeded with correct domain/indicator codes (T-x.x, A-x.x)  # DONE — seed + seed-taxonomy tests
      - Score weights (60/40, 20/10/10) and rank levels seeded                     # DONE
      - Idempotent seed script; re-runnable                                        # DONE
      - Full IndicatorLevelDescription per rank level from PDFs                   # RESIDUAL

  - id: SEIP-API-001
    title: Backend evidence-workflow API (15 of 27 contract operations)
    status: done                # 2026-07-17 — merged develop 3db868d; handoff SEIP-API-001.md
    branch: feat/SEIP-API-001-evidence-workflow
    dependencies: [SEIP-ARCH-002, SEIP-DB-001]
    notes: >
      Not originally on the board — created mid-session after auditing the plan
      against actual repo state and finding apps/api completely unwritten despite
      locked contracts + built schema. packages/backend-shared, packages/auth,
      packages/database, apps/api (auth, taxonomy, evidence, mappings routes).
      CCR-003 (current-school resolution) and CCR-004 (upload-complete metadata)
      raised and applied. 12 of 27 operations (cycles/rounds/scoring) explicitly
      deferred to a future SEIP-API-002. Verified end-to-end against real
      Postgres + MinIO: 6 apps/api integration tests, 6 packages/database
      integration tests, 13 packages/auth unit tests, all passing.
    acceptance_criteria:
      - Auth (login, me) with real password hashing + JWT           # DONE
      - Taxonomy read endpoints (frameworks, categories)             # DONE
      - Evidence CRUD + two-phase MinIO upload                       # DONE
      - Governed mapping create/confirm/reject/revoke                # DONE
      - Tenancy isolation proven, not just asserted                  # DONE — cross-school RES-001 test
      - Permission matrix enforced from permissions.yaml at runtime  # DONE — SEC-TEN-5

  - id: SEIP-QA-003
    title: Permission matrix tests (arms the permission-tests CI gate)
    status: done                # 2026-07-17 — verified with API-001; handoff SEIP-QA-003.md
    branch: feat/SEIP-API-001-evidence-workflow
    dependencies: [SEIP-API-001]
    notes: >
      tests/security/permission-matrix.test.mjs — generated FROM permissions.yaml
      (not hand-duplicated), 78+ assertions across 13 operations x 6 roles.
      Found and fixed a real bug: area_admin's X-School-Id resolution rejected
      every header (no direct school membership to match against) instead of
      checking area-based access. Arms the previously-empty permission-tests gate.
    acceptance_criteria:
      - Test cases generated from permissions.yaml, not hand-picked  # DONE
      - Every implemented operation x role combination covered        # DONE — 13 ops x 6 roles
      - area_admin read-only boundary verified                        # DONE
      - CCR-003 header semantics verified (single vs multi-membership) # DONE

  - id: SEIP-UI-001
    title: Evidence submission flow implementation
    status: done                # 2026-07-17 — merged develop 3db868d; verified re-run; handoff SEIP-UI-001.md
    branch: feat/SEIP-API-001-evidence-workflow
    dependencies: [SEIP-ARCH-002, SEIP-DB-001, SEIP-API-001]
    notes: >
      apps/web (Vite+React+TS, ADR-0006) — full S1-S7 flow against the real API.
      Verified end-to-end through a real browser (not just tests): real MinIO
      upload, real ว9 taxonomy chips (T-1.1..T-3.3 from the actual ก.ค.ศ. PDF),
      server-side DB+MinIO verification after each step. 12 unit/component
      tests. Mobile viewport (375px) verified: no overflow, 56px touch targets.
    acceptance_criteria:
      - Teacher submits evidence with minimal fields   # DONE — verified live in browser
      - Responsive mobile support                       # DONE — verified live, 375px viewport
      - Accessibility checks pass                        # DONE — semantic roles/aria verified via accessibility tree

  - id: SEIP-OPS-002
    title: Fix develop CI break from the API-001+UI-001 merge
    status: done                # 2026-07-17 — fixed 556958c, verified green on GitHub Actions run 29549242802
    dependencies: [SEIP-API-001, SEIP-UI-001]
    notes: >
      First real exercise of the integration/permission-tests gates' Postgres+MinIO
      service containers on GitHub's own runners surfaced 2 latent bugs: (1)
      contract-compatibility never ran `npm ci`, so its codegen-diff step's bare
      js-yaml import had nothing to resolve; (2) MinIO's `--entrypoint sh` override
      replaced startup with a no-op shell — GitHub Actions services can't pass
      MinIO's required `server /data` command, so it now starts as a manual step.
      Root-caused via GitHub's jobs API (log download needed elevated auth this
      session doesn't have) + local reproduction in isolated dirs and real Docker,
      not guessed. Full test suite re-verified against disposable CI-matching
      containers before push.
    acceptance_criteria:
      - Root cause identified for all 3 failing gates, not just patched around  # DONE
      - Fix verified locally against disposable, CI-matching infra              # DONE
      - develop green on GitHub Actions (not just locally)                     # DONE — run 29549242802, 12/12 jobs

  - id: SEIP-API-002
    title: Cycles + committee scoring (remaining 11 of 27 contract operations)
    status: done                # 2026-07-17 — merged develop 82a6bd7; re-verified green
    branch: feat/SEIP-API-002-cycles-scoring
    dependencies: [SEIP-API-001, SEIP-DB-001]
    notes: >
      Closes the gap SEIP-API-001 explicitly deferred. cycles tag (listCycles,
      createCycle, getCycle, updateCycle, createRound, updateRound) + scoring tag
      (listAssignments, createAssignment, getAssignment, submitMyScores,
      getAssignmentResults) — all 27 contract operations now implemented.
      Also un-stubbed the evaluator:committee grant on 4 evidence/mapping
      endpoints (previously hard-denied with "scoring deferred" — the DB-001
      schema already modeled EvaluationCycle/Round/Assignment/CommitteeMember/
      IndicatorScore/RoundResult in full; this is the API layer over it).
      Scoring formula (part1 standard=equal-weight average, part2 challenge=
      maxPoints-weighted, total=part1*60%+part2*40%) derived from
      evaluation-framework.md + seeded ScoreWeight/Indicator data, not assumed —
      documented in scoring.ts. permission-matrix.test.mjs extended from 13 to 23
      operations x 6 roles; caught a real own-grant inconsistency (RES-001 vs the
      established PERM-001 precedent) before merge.
    acceptance_criteria:
      - All 27 openapi.yaml operations implemented                              # DONE
      - Per-evaluator scoring, 3-member committee, workload gate enforced       # DONE
      - ≥70% from EVERY evaluator individually — proven, not just averaged      # DONE — scoring-flow.test.mjs
      - Round state machine (planned->open->scoring->closed) enforced           # DONE — CYCLE-001
      - evaluator:committee grant wired up on evidence/mappings                 # DONE
      - Verified end-to-end against disposable Postgres+MinIO                  # DONE — 12 apps/api + 6 database + 3 security tests
      - permission-matrix sweep extended to cover the new operations            # DONE — 13->23 ops

# =============================================================================
# Sprint 2+ roadmap (added 2026-07-18) — non-overlapping paths + dependency DAG
# Coordination while Claude Code is sole implementer (ADR-0004):
#   1. At most ONE task status=in_progress that lists a given primary_path prefix.
#   2. Parallel only when primary_paths sets are disjoint (see Parallel lanes).
#   3. Branch: feat/<task-id>-<slug> off latest develop; no force-push.
#   4. Contract edits require CCR + version bump (gate:contracts enforces).
#   5. Shared packages (database/auth/backend-shared): serialize — never two
#      in_progress tasks that both list packages/database/** as primary.
# Parallel lanes (safe concurrent pairs after deps met):
#   Lane A: SEIP-OPS-003  (infra/**, docker-compose*.yml, docs ops notes)
#   Lane B: SEIP-DB-003   (prisma/seed*.mjs or prisma/data/** only)
#   Lane C: SEIP-WORKER-001 (apps/worker/** + outbox migration if needed)
#   Lane D: SEIP-UI-002   (apps/web director) — NOT parallel with UI-003
# Serial after contracts: ARCH-003 → API-003 → WORKER-002 / UI-004
# AI chain: OPEN-3 closed (ADR-0007 local_heuristic); ARCH-005 → API-004 → UI-005 done on REPORTS-AI
# =============================================================================

  - id: SEIP-PLAN-001
    title: "Sprint 2+ path map and anti-collision rules (meta)"
    status: done
    notes: >
      Documents the dependency DAG and primary_paths for the tasks below so
      Claude Code (or any co-pilot) does not open two branches that edit the
      same tree. No code deliverable — this task is the board itself.
    acceptance_criteria:
      - Board lists worker, multi-role UI, reports, AI, ops with deps and paths  # DONE
      - Parallel lanes A–D defined                                                # DONE

  # --- Wave A: can start now (path-disjoint; prefer OPS-003 before WORKER compose) ---

  - id: SEIP-OPS-003
    title: Staging-oriented compose + secrets/TLS/backup checklist (on-prem)
    status: done                # 2026-07-18 — handoff SEIP-OPS-003.md; branch feat/SEIP-OPS-003-staging-ops
    wave: A
    sprint: 2
    branch: feat/SEIP-OPS-003-staging-ops
    dependencies: [SEIP-OPS-002, SEIP-API-002]
    primary_paths:
      - infra/**
      - docker-compose.staging.yml
      - docs/project/ops-runbook.md
      - .env.example
    avoid_paths:
      - apps/api/src/**
      - apps/web/src/**
      - prisma/schema.prisma
    notes: >
      ADR-0005 on-prem. Dev compose stays; add staging profile with non-dev secrets
      pattern, healthchecks, backup/restore procedure for Postgres+MinIO, TLS notes.
      Does NOT implement business features. Own compose layout before WORKER-001
      adds a worker service entry (WORKER-001 depends on this).
    acceptance_criteria:
      - Staging compose (or documented profile) for postgres + minio + api + web
      - Secrets never committed; .env.example documents required vars
      - Backup/restore checklist for Postgres and MinIO object store
      - TLS termination guidance for reverse proxy (no fake certs in git)

  - id: SEIP-DB-003
    title: Seed full IndicatorLevelDescription text from ว9/ว10 (residual DB-002)
    status: done                # 2026-07-18 - bundled on feat/SEIP-REPORTS-AI; handoff SEIP-REPORTS-AI.md
    wave: A
    sprint: 2
    branch: feat/SEIP-DB-003-level-descriptions
    dependencies: [SEIP-DB-002]
    primary_paths:
      - prisma/seed.mjs
      - prisma/data/**
      - tests/backend/seed-taxonomy.test.mjs
    avoid_paths:
      - prisma/schema.prisma
      - apps/**
    notes: >
      Data-only. Do not change schema. Source = evaluation-framework.md + official
      PDFs (not invent wording). Parallel-safe with OPS-003 and UI tasks.
      NOT parallel with WORKER-001 if worker needs a schema migration in the same
      window — finish DB-003 first or use WORKER-001's own migration only.
    acceptance_criteria:
      - Level descriptions seeded for teacher + admin frameworks (idempotent)
      - Tests assert coverage for scored indicators × rank tiers (or documented subset)
      - No schema migration required

  # --- Wave B: worker foundation ---

  - id: SEIP-WORKER-001
    title: Async worker — outbox consumer, virus scan, duration probe, storage GC
    status: done                # 2026-07-18 - bundled on feat/SEIP-REPORTS-AI; handoff SEIP-REPORTS-AI.md
    wave: B
    sprint: 2
    branch: feat/SEIP-WORKER-001-async-pipeline
    dependencies: [SEIP-API-001, SEIP-DB-001, SEIP-OPS-003]
    primary_paths:
      - apps/worker/**
      - packages/database/src/repositories/outbox.ts
      - packages/database/src/repositories/jobs.ts
      - prisma/migrations/*_outbox*
    secondary_paths:
      - packages/backend-shared/**
      - apps/api/src/routes/evidence.ts
      - docker-compose.yml
    notes: >
      Single worker process, multiple job types (scan, duration, outbox dispatch, GC).
      API only enqueues / writes outbox — no second job runner later for AI/reports.
      If outbox table migration is needed, this task owns it (not DB-003).
      Secondary touch of evidence.ts only to emit outbox after completeFileUpload
      if not already present — keep diff minimal.
    acceptance_criteria:
      - apps/worker runs against same DATABASE_URL + S3_* as api
      - After completeFileUpload, scan job transitions scan_status pending→clean|blocked
      - Video duration probe fills duration_seconds when missing (UPL-003 authority)
      - Outbox events from events.yaml are dispatched idempotently
      - Integration test with real Postgres (+ MinIO where needed)
      - No separate queue for future AI/report jobs — extend this worker

  - id: SEIP-API-003a
    title: Presigned download URL when scan_status=clean (UPL-006)
    status: done                # 2026-07-18 - handoff SEIP-API-003a.md; wave branch feat/SEIP-WAVE-B-CF
    wave: B
    sprint: 2
    branch: feat/SEIP-API-003a-safe-download
    dependencies: [SEIP-WORKER-001]
    primary_paths:
      - apps/api/src/routes/evidence.ts
      - apps/api/src/lib/s3.ts
      - apps/api/test/**
    avoid_paths:
      - apps/worker/**
      - apps/web/**
    notes: >
      Small API follow-up after worker can mark clean. Do not parallel with other
      evidence.ts edits.
    acceptance_criteria:
      - download_url issued only when scan_status=clean
      - Short-lived presigned GET; never expose storageUri
      - Test covers blocked/pending → null url

  # --- Wave C: multi-role UI (serial UI-002 → UI-003 on apps/web) ---

  - id: SEIP-UI-002
    title: Director/admin UI — cycles, rounds, committee assignment
    status: done                # 2026-07-18 - bundled on feat/SEIP-REPORTS-AI; handoff SEIP-REPORTS-AI.md
    wave: C
    sprint: 2
    branch: feat/SEIP-UI-002-director-cycles
    dependencies: [SEIP-API-002, SEIP-UI-001]
    primary_paths:
      - apps/web/src/pages/director/**
      - apps/web/src/pages/admin/**
      - apps/web/src/App.tsx
      - apps/web/src/hooks/**
    avoid_paths:
      - apps/api/**
      - apps/worker/**
      - prisma/**
    notes: >
      Consumes existing cycles + createAssignment APIs only (no new contract unless CCR).
      Role-gated nav from getCurrentUser (RequireRole + hasRole(), the app's first
      role gate). Rounds have no route of their own (no GET /rounds/{roundId} in the
      contract) — shown inline on CycleDetailPage instead; only assignment management
      (real GET /rounds/{roundId}/assignments) gets its own route. Known gap: no
      personnel-listing endpoint exists, so evaluatee/committee fields are raw-uuid
      inputs (flagged in-form + in the handoff, not silently worked around). Hit and
      recovered from a real shared-working-directory collision with a concurrent
      session mid-task — see handoff's infra-hazard section; moved to an isolated
      git worktree for the remainder of the work. Do NOT start UI-003 until this
      merges — both touch App.tsx routing.
    acceptance_criteria:
      - Director/school_admin can create/open cycle and rounds                        # DONE — verified live in browser
      - Create 3-member assignment (1 chair + 2 members) with validation errors shown  # DONE — 4 component tests + live verification
      - Unauthorized roles cannot open these routes (UI hide + API still enforces)     # DONE — teacher redirected away, verified live
      - Uses generated OpenAPI types only                                              # DONE

  - id: SEIP-UI-003
    title: Evaluator UI — submit scores + view assignment results
    status: done                # 2026-07-18 - handoff SEIP-UI-003.md; branch feat/SEIP-UI-003-evaluator-scoring
    wave: C
    sprint: 2
    branch: feat/SEIP-UI-003-evaluator-scoring
    dependencies: [SEIP-UI-002, SEIP-API-002]
    primary_paths:
      - apps/web/src/pages/evaluator/**
      - apps/web/src/App.tsx
      - apps/web/src/components/scoring/**
    avoid_paths:
      - apps/api/**
      - apps/worker/**
    notes: >
      Depends UI-002 for shared shell/nav patterns. Uses submitMyScores /
      getAssignmentResults. Shows per-evaluator ≥70% and workload gate clearly.
    acceptance_criteria:
      - Committee member can submit full indicator set for an assignment
      - SCORE-002 / SCORE-005 surfaced as Thai errors
      - Evaluatee cannot see results until round closed (matches API)
      - Unit/component tests for score form validation

  - id: SEIP-UI-003b
    title: Teacher UI — scan status + download when clean
    status: done                # 2026-07-18 - handoff SEIP-UI-003b.md; wave branch feat/SEIP-WAVE-B-CF
    wave: C
    sprint: 2
    branch: feat/SEIP-UI-003b-scan-download
    dependencies: [SEIP-WORKER-001, SEIP-API-003a, SEIP-UI-001]
    primary_paths:
      - apps/web/src/pages/EvidenceDetailPage.tsx
      - apps/web/src/pages/EvidenceListPage.tsx
    avoid_paths:
      - apps/web/src/pages/director/**
      - apps/web/src/pages/evaluator/**
    notes: >
      Narrow UI polish; path-disjoint from director/evaluator folders. Avoid
      simultaneous App.tsx edits with UI-002/003.
    acceptance_criteria:
      - List/detail reflect scan_status updates (poll or refresh)
      - Download control only when clean + url present

  # --- Wave D: reports (contract → API → worker job → UI) ---

  - id: SEIP-ARCH-003
    title: Reports contract v0.2 (PA1/PA2/PA3 surface) + permissions
    status: done                # 2026-07-18 - bundled on feat/SEIP-REPORTS-AI; handoff SEIP-REPORTS-AI.md
    wave: D
    sprint: 2
    branch: feat/SEIP-ARCH-003-reports-contract
    dependencies: [SEIP-API-002, SEIP-UI-000]
    primary_paths:
      - docs/contracts/openapi.yaml
      - docs/contracts/permissions.yaml
      - docs/contracts/error-codes.yaml
      - docs/contracts/events.yaml
      - docs/reviews/CCR-*
    avoid_paths:
      - apps/**
      - prisma/**
    notes: >
      Contract-only. Inventory official form fields first. Bump version per
      contract-policy. Parallel-safe with UI-002/003 and WORKER-001 (docs only).
    acceptance_criteria:
      - CCR filed and applied; openapi version bumped appropriately
      - Report endpoints + DTOs listed; authz matrix complete for new operationIds
      - gate:contracts green

  - id: SEIP-API-003
    title: Reports API — create/get report + section refs (no binary layout engine)
    status: done                # 2026-07-18 - bundled on feat/SEIP-REPORTS-AI; handoff SEIP-REPORTS-AI.md
    wave: D
    sprint: 2
    branch: feat/SEIP-API-003-reports-api
    dependencies: [SEIP-ARCH-003, SEIP-DB-001]
    primary_paths:
      - apps/api/src/routes/reports.ts
      - packages/database/src/repositories/reports.ts
      - apps/api/test/reports*.test.mjs
      - tests/security/permission-matrix.test.mjs
    avoid_paths:
      - apps/web/**
      - apps/worker/**
    notes: >
      Uses Report/ReportSectionRef tables from DB-001. Heavy PDF/layout generation
      is WORKER-002, not inline in HTTP handler. Serialize vs API-003a (different
      route files — OK in parallel if no shared server.ts conflict; prefer after 003a).
    acceptance_criteria:
      - Endpoints per ARCH-003 contract
      - Only confirmed mappings eligible for official sections
      - Permission matrix extended; tenancy tests pass

  - id: SEIP-WORKER-002
    title: Report generation job type on existing worker
    status: done                # 2026-07-18 - bundled on feat/SEIP-REPORTS-AI; handoff SEIP-REPORTS-AI.md
    wave: D
    sprint: 2
    branch: feat/SEIP-WORKER-002-report-jobs
    dependencies: [SEIP-WORKER-001, SEIP-API-003]
    primary_paths:
      - apps/worker/src/jobs/report*
      - packages/database/src/repositories/reports.ts
    avoid_paths:
      - apps/web/**
      - apps/api/src/routes/**
    notes: >
      Extends WORKER-001 — same process, new job type. Do not create apps/report-runner.
    acceptance_criteria:
      - Job builds report payload from scores + confirmed mappings
      - Idempotent retries without duplicate issued reports
      - Integration test with fixture assignment results

  - id: SEIP-UI-004
    title: Reports UI — request/download PA forms for allowed roles
    status: done                # 2026-07-18 - bundled on feat/SEIP-REPORTS-AI; handoff SEIP-REPORTS-AI.md
    wave: D
    sprint: 2
    branch: feat/SEIP-UI-004-reports
    dependencies: [SEIP-API-003, SEIP-UI-002]
    primary_paths:
      - apps/web/src/pages/reports/**
      - apps/web/src/App.tsx
    avoid_paths:
      - apps/api/**
      - apps/worker/**
    notes: >
      After UI-002 shell exists. Serial with other App.tsx work.
    acceptance_criteria:
      - Role-appropriate report list/detail
      - Clear empty/error/generating states

  # --- Wave E: AI mapping (OPEN-3 closed by ADR-0007; implemented on REPORTS-AI) ---

  - id: SEIP-ARCH-005
    title: "ADR — AI provider + PDPA residency (closes OPEN-3)"
    status: done                # 2026-07-18 - bundled on feat/SEIP-REPORTS-AI; handoff SEIP-REPORTS-AI.md
    wave: E
    sprint: 2
    branch: feat/SEIP-ARCH-005-ai-adr
    dependencies: [SEIP-ARCH-002]
    primary_paths:
      - docs/decisions/ADR-0007-ai-provider-pdpa.md
      - docs/project/PROJECT_STATE.md
    notes: >
      BLOCKED until user decides provider and whether any personal data may leave
      on-prem hardware (ADR-0005 tension). No code before ADR Accepted.
    acceptance_criteria:
      - ADR Accepted by user
      - Explicit allow/deny for sending evidence text/images off-box
      - OPEN-3 closed in PROJECT_STATE

  - id: SEIP-API-004
    title: "AI mapping suggest endpoint (contract already reserved)"
    status: done                # 2026-07-18 - bundled on feat/SEIP-REPORTS-AI; handoff SEIP-REPORTS-AI.md
    wave: E
    sprint: 2
    branch: feat/SEIP-API-004-ai-suggest
    dependencies: [SEIP-ARCH-005, SEIP-API-001, SEIP-WORKER-001]
    primary_paths:
      - docs/contracts/openapi.yaml
      - apps/api/src/routes/mappings.ts
      - apps/worker/src/jobs/ai-mapping*
      - packages/database/src/repositories/mappings.ts
    notes: >
      CCR if contract surface changes beyond reserved ai_suggested.
      Worker runs model call; API only enqueues + returns suggestion ids.
      Human confirm still required (existing actOnMapping).
    acceptance_criteria:
      - POST suggest creates mapping_source=ai_suggested only
      - No auto-confirm; permissions unchanged for confirm
      - PDPA constraints from ADR-0007 enforced
      - Tests for suggest + confirm path

  - id: SEIP-UI-005
    title: Teacher UI — show AI suggestions + confirm/reject
    status: done                # 2026-07-18 - bundled on feat/SEIP-REPORTS-AI; handoff SEIP-REPORTS-AI.md
    wave: E
    sprint: 2
    branch: feat/SEIP-UI-005-ai-suggestions
    dependencies: [SEIP-API-004, SEIP-UI-001]
    primary_paths:
      - apps/web/src/pages/EvidenceSubmitPage.tsx
      - apps/web/src/pages/EvidenceDetailPage.tsx
      - apps/web/src/components/mapping/**
    avoid_paths:
      - apps/web/src/pages/director/**
      - apps/web/src/pages/evaluator/**
    notes: >
      Light touch on teacher pages; avoid parallel with large teacher refactors.
    acceptance_criteria:
      - Suggestions labeled as AI; user must confirm
      - Errors/PDPA notices in Thai where appropriate

  # --- Wave F: polish ---

  - id: SEIP-QA-004
    title: Browser e2e smoke (login → submit evidence) in CI
    status: done                # 2026-07-18 - handoff SEIP-QA-004.md; wave branch feat/SEIP-WAVE-B-CF
    wave: F
    sprint: 2
    branch: feat/SEIP-QA-004-e2e-smoke
    dependencies: [SEIP-UI-001, SEIP-OPS-002]
    primary_paths:
      - tests/e2e/**
      - .github/workflows/ci.yml
    notes: >
      Playwright/Cypress smoke against compose stack. Path-disjoint from feature
      work if it only adds tests/e2e + workflow job.
    acceptance_criteria:
      - One green e2e path on CI with Postgres+MinIO+api+web
      - Documented in QUALITY-GATES.md

  - id: SEIP-OPS-004
    title: Login rate limiting + security headers at edge
    status: done                # 2026-07-18 - handoff SEIP-OPS-004.md; branch feat/SEIP-OPS-004-edge-security
    wave: F
    sprint: 2
    branch: feat/SEIP-OPS-004-edge-security
    dependencies: [SEIP-OPS-003]
    primary_paths:
      - infra/**
      - apps/api/src/plugins/**
    notes: >
      Prefer reverse-proxy rate limit; api plugin only if unavoidable.
      Avoid parallel with large api route work.
    acceptance_criteria:
      - Login brute-force limited
      - Safe serving headers documented/enforced where applicable

  - id: SEIP-SEC-002
    title: Audit critical fixes — session lifecycle (CCR-008), committee integrity, real lint gate
    status: done                # 2026-07-18 — claude; branch feat/SEIP-SEC-002-audit-critical-fixes
    sprint: 2
    branch: feat/SEIP-SEC-002-audit-critical-fixes
    dependencies: [SEIP-OPS-004]
    primary_paths:
      - apps/api/src/routes/auth.ts
      - apps/api/src/routes/scoring.ts
      - apps/web/src/api/client.ts
      - apps/web/src/hooks/useAuth.tsx
      - apps/web/eslint.config.js
      - docs/contracts/**
    notes: >
      Closes the 2026-07-18 code-status audit's critical items: (1) CCR-008
      /auth/refresh (rotation + reuse detection) + /auth/logout (server-side
      revocation) wiring the already-built RefreshToken layer, with transparent
      refresh-on-401 in the web client; (2) committee seats now require an active
      evaluator/director membership at the school and the evaluatee can never sit
      on their own committee; (3) apps/web gets real ESLint (ts-eslint +
      react-hooks + jsx-a11y) so the CI lint gate stops no-opping; (4) full-suite
      verified run recorded in QUALITY-GATES.md. Branch protection required-check
      additions still need the user's GitHub access.
    acceptance_criteria:
      - Refresh rotates + replay kills family; logout revokes server-side   # DONE — 4 integration tests
      - Committee school+role eligibility + self-scoring block              # DONE — integration test
      - Lint gate runs real ESLint with 0 findings                          # DONE — 3 real a11y/correctness bugs fixed
      - Full suite green + recorded                                         # DONE — QUALITY-GATES.md 2026-07-18 table

  - id: SEIP-CLEANUP-H2
    title: Lazy evidence file download URL (no presign on getEvidence)
    status: done
    sprint: 2
    branch: feat/cleanup-H2-lazy-download
    dependencies: [SEIP-API-003a, SEIP-UI-003b]
    primary_paths:
      - docs/contracts/openapi.yaml
      - docs/contracts/permissions.yaml
      - apps/api/src/routes/evidence.ts
      - apps/web/src/pages/EvidenceDetailPage.tsx
      - apps/api/test/evidence-flow.test.mjs
    notes: >
      CCR-010 / openapi 2.5.0: getEvidenceFileDownloadUrl; serializeFile never
      calls MinIO. UPL-006 enforced at issue time. Handoff .ai-team/handoffs/SEIP-CLEANUP-H2.md
    acceptance_criteria:
      - getEvidence / completeFileUpload download_url always null
      - GET .../files/{fileId}/download-url issues URL only when clean
      - pending/blocked → UPL-006 (423)
      - UI downloads via lazy endpoint on click

  - id: SEIP-CLEANUP-M2
    title: Shared ReportPayloadV1 type across worker/API/PDF/UI
    status: done
    sprint: 2
    branch: feat/cleanup-M2-report-payload-v1
    primary_paths:
      - packages/backend-shared/src/report-payload-v1.ts
      - packages/database/src/repositories/reports.ts
      - apps/api/src/lib/pa-report-pdf.ts
      - apps/api/src/routes/reports.ts
      - apps/web/src/pages/reports/ReportDetailPage.tsx
      - docs/contracts/openapi.yaml
    notes: >
      CCR-011 / openapi 2.6.0. Ends Record<string, unknown> casts on report payload.
      Handoff .ai-team/handoffs/SEIP-CLEANUP-M2.md
    acceptance_criteria:
      - Typed ReportPayloadV1 in backend-shared
      - generateReportPayload + getReport + PDF use it
      - OpenAPI ReportPayload + web generated types

  - id: SEIP-CLEANUP-M3
    title: App capability flags from memberships (no ad-hoc role arrays)
    status: done
    sprint: 2
    branch: feat/cleanup-M3-capabilities
    primary_paths:
      - apps/web/src/lib/capabilities.ts
      - apps/web/src/hooks/useAuth.tsx
      - apps/web/src/App.tsx
    notes: >
      manageCycles / scoreAsCommittee / viewReports / createReports / governMappings
      derived once from memberships. Handoff .ai-team/handoffs/SEIP-CLEANUP-M3.md
    acceptance_criteria:
      - No DIRECTOR_ROLES / REPORT_NAV_ROLES / EVALUATOR_NAV_ROLES in App.tsx
      - Nav and route gates use capability flags
      - CAPABILITY_ROLES comments cite permissions.yaml ops

  - id: SEIP-CLEANUP-M4
    title: Login rate limiter process-global singleton + multi-instance docs
    status: done
    sprint: 2
    branch: feat/cleanup-M4-login-rate-singleton
    primary_paths:
      - apps/api/src/lib/login-rate-limit.ts
      - apps/api/src/server.ts
      - apps/api/test/login-rate-limit.test.mjs
      - docs/project/ops-runbook.md
    notes: >
      globalThis singleton; tests install/restore process state. Edge is primary
      for multi-instance — Redis not a launch blocker. Handoff SEIP-CLEANUP-M4.md
    acceptance_criteria:
      - Single process-wide limiter used by auth routes
      - Tests mutate singleton and restore
      - Multi-instance documented as edge-primary MVP
