{
  "name": "seip",
  "private": true,
  "version": "0.0.0",
  "description": "School Evidence Intelligence Platform — root gate-runner (SEIP-QA-001). Workspace/build scripts arrive with Sprint 1; adding scripts.{format:check,lint,typecheck,test:unit,test:integration,build} auto-arms the matching CI gate.",
  "license": "UNLICENSED",
  "scripts": {
    "gate:ownership": "node scripts/orchestration/validate-ownership.mjs",
    "gate:contracts": "node scripts/security/validate-contracts.mjs",
    "gate:secret-scan": "node scripts/security/run-gitleaks.mjs",
    "gate:dep-audit": "npm audit --audit-level=high"
  }
}
